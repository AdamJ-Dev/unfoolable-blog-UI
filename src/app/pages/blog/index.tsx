import { Blog } from '../../types/blog';

type BlogPageProps = {
  blog: Blog;
};

const BlogPage: React.FC<BlogPageProps> = () => {
  return (
    <>
      <div>hey</div>
    </>
  );
};

export default BlogPage;
